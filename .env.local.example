# Copy this file to .env.local and fill in your Azure AD app settings
# SPA Redirect URI must be added in Azure Portal under Authentication

VITE_MSAL_CLIENT_ID=00000000-0000-0000-0000-000000000000
VITE_MSAL_TENANT_ID=your-tenant-id-or-common
VITE_MSAL_REDIRECT_URI=http://localhost:8080/

# Optional: override scopes (defaults set in code)
# VITE_GRAPH_SCOPES=Sites.ReadWrite.All,Files.ReadWrite.All

# SharePoint site and Excel file configuration
# If omitted, site resolves via API to /sites/pipe-inspection on kzprimeestate.sharepoint.com
# Example file path points to an Excel file under the default document library
# NOTE: Path must start with a leading slash and include library name (e.g., /Shared Documents/...)
VITE_SP_SITE_ID=
VITE_SP_FILE_PATH=/Shared Documents/UPLOADS/pipe_inspection.xlsm
